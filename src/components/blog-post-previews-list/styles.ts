import styled from "~/utils/styled"

export const BlogPostPreviewListWrapper = styled.div`
  margin: 3.2rem auto;
  max-width: 60em;
`

export const PaginationSplitter = styled.span({
  flexGrow: 1,
})

export const PaginationButtonsHolder = styled.div({
  display: "flex",
})
